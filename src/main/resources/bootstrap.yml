spring:
  profiles:
    active: dev
  application:
    name: auth-service
  cloud:
    config:
      uri: http://localhost:9000

server:
  port: 9005
management:
  endpoints:
    web:
      exposure:
        include: refresh

logging:
  level:
    org.springframework.web: DEBUG